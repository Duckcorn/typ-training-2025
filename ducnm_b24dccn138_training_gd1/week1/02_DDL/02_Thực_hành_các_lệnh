**Mục đích:**  
Tạo bảng <span style="color:blue;">BOOKS</span> trong cơ sở dữ liệu để lưu thông tin về sách.

**Cú pháp SQL:**
```sql
CREATE TABLE BOOKS (
    book_id INT,
    tittle VARCHAR(200) NOT NULL,
    author VARCHAR(100),
    category VARCHAR(50),
    year_published INT,
    CONSTRAINT pk_BOOKS PRIMARY KEY (book_id)
);
```
